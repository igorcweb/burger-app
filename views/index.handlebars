<img class="mt-5" src="/assets/images/burger.png" alt="">
<h1 class="my-4">Eat da Burger!</h1>
<div class="container">

</div>
<div class="list d-flex flex-column flex-md-row align-items-center align-items-sm-start justify-content-between mx-auto">
  <ul class="mb-3">
    {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block }} {{/unless}} {{/each}}
  </ul>
  <ul class="">
    {{#each burgers}} {{#if devoured}} {{> burgers/burger-block }} {{/if}} {{/each}}
  </ul>

</div>
<form class="mt-2 insert-form">
  <div class="form-group">
    <label for="burger">Enter the burger that your heart desires:</label>
    <input type="text" name="burger" id="burger">
  </div>
  <button class="btn btn-success">Add da Burger</button>
</form>