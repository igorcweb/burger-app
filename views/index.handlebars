<img class="mt-5" src="/assets/images/burger.png" alt="">
<h1 class="my-4">Eat da Burger!</h1>
<div class="container">

</div>
<div class="list d-flex flex-column flex-md-row align-items-center justify-content-between mx-auto">
  <ul class="mb-3">
    {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block }} {{/unless}} {{/each}}
  </ul>
  <ul class="">
    {{#each burgers}} {{#if devoured}} {{> burgers/burger-block }} {{/if}} {{/each}}
  </ul>

</div>
<form>
  <div class="form-group">
    <textarea name="" id="" cols="30" rows="2"></textarea>
  </div>

  <button class="btn btn-primary">Submit</button>
</form>